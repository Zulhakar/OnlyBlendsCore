# Custom Nodes Template
It's a Custom Node Editor, separated from and for OnlyBlends.Mixer and future projects.

## Features

This is not only for my projects or a template for devs, it has its own use case to spawn objects from Geometry Node Modifiers.
For example on different frames with different input parameters for random instances. 